# dependencies
node_modules/

# build output
out/
dist/
release/

# local state
*.local
.env
.env.*

# editors / OS
.DS_Store
Thumbs.db
.idea/
*.log

# test coverage
coverage/

# gstack local artifacts / security reports
.gstack/

# local QA captures
shots/
.playwright-mcp/

# Local-only notes and agent config. These were covered by a global gitignore
# only, so a single `git add -A` on another machine would have published them —
# increments and plans routinely contain personal context, and ui-shot.mjs
# hardcodes a home directory path.
inc/
plan/
план/
CLAUDE.md
.claude/
scripts/ui-shot.mjs
